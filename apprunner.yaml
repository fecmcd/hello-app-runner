version: 1.0
runtime: python3
build:
  commands:
    build:
    - yum install -y pycairo
    - pip install -r requirements.txt
    - pwd
    - ls -la
    - mv config .aws/config
    - ls -la .aws/config
run:
  command: python app.py
  network:
    port: 8080
