version: 1.0
runtime: python3
build:
  commands:
    build:
    - yum install -y pycairo
    - pip install -r requirements.txt
    - aws s3 cp s3://felipecm-my-bucket-1/config .aws/config
    - aws rds generate-db-auth-token --hostname apprunnerdeletelater-instance-1.caof5xfkvk2e.ap-southeast-2.rds.amazonaws.com --port 3306 --username admin --profile instancecrossaccount
run:
  command: python app.py
  network:
    port: 8000
